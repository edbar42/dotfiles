#!/bin/bash

# Check if an argument was provided
if [ $# -eq 0 ]; then
  echo "Error: No target provided" >&2
  exit 1
fi

target="$1"
target_dir="$HOME/code/java"

mv $target $target_dir
pushd $target_dir
unzip $target
cd "$target_dir/$(basename $target .zip)"
idea .&

