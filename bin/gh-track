#!/bin/bash

REPO_NAME="$(basename $PWD)"

SSH_URL=$(gh repo view edbar42/$REPO_NAME --json sshUrl | jq ."sshUrl" | sed "s/\"//g")
echo Adding $SSH_URL as remote origin
echo You should see it listed as origin below:

git remote add origin $SSH_URL
git remote -v
