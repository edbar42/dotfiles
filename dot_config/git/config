[alias]
	fix = commit --amend
	lg = log --decorate=full --oneline
	st = status
	cmt = commit
	ok = rebase --continue
  patch = commit --amend --no-edit

[commit]
	gpgsign = true

[core]
	autocrlf = input
	editor = nvim
	fileMode = false
	pager = delta

[delta]
	line-numbers = true
	side-by-side = true
  navigate = true

[gpg]
	format = ssh

[init]
	defaultBranch = master

[pull]
	rebase = true

[push]
	default = current

[rebase]
	autoStash = true

[user]
	name = Eduardo Barroso
	email = edbar42@proton.me
	signingkey = ~/personal/auth/ssh/github.pub
