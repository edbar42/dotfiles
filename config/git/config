[alias]
	fix = commit --amend
	short = log --decorate=full --oneline
	st = status -s
	cmt = commit
	ok = rebase --continue

[commit]
	gpgsign = true

[core]
	autocrlf = input
	editor = nvim
	fileMode = false
	pager = delta

[delta]
	line-numbers = true
	side-by-side = true

[gpg]
	format = ssh

[init]
	defaultBranch = main

[pull]
	rebase = true

[push]
	default = current

[rebase]
	autoStash = true

[user]
	name = Eduardo Barroso
	email = dev@edbar.xyz
	signingkey = ~/.ssh/id_ed25519.pub
